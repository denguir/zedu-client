# NGINX
SCHOOL_NAME=ISND
ZEUS_DOMAIN_NAME=zeus.local # add "127.0.0.1	    zeus.local" in /etc/hosts if localhost
THOT_DOMAIN_NAME=thot.local # add "127.0.0.1	    thot.local" in /etc/hosts if localhost

# MYSQL
MYSQL_ROOT_PASSWORD=azerty
MYSQL_HOST=mariadb
MYSQL_DATABASE=ZeusEdu
# PHPMYADMIN
PMA_ARBITRARY=1
PMA_HOST=mariadb